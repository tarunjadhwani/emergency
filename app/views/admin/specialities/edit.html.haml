- content_for :title, @admin_speciality.name

%h3= "Editing #{@admin_speciality.name}"

.well
  = render 'form'

= link_to 'Show', [:admin, @admin_speciality]
\|
= link_to 'Back', admin_specialites_path

:javascript
  $(".edit_speciality").validate();
